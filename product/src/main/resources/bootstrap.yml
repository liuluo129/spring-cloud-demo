spring:
  application:
    name: product
  cloud:
    config:
      uri: http://localhost:8083
      name: db
      profile: pro

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/