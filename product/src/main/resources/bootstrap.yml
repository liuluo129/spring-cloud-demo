spring:
  application:
    name: product
  cloud:
    config:
      uri: http://localhost:8083
      name: book
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/